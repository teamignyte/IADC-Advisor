# iadc-advisor Plugin Conversion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert the flatten-into-repo deliverable into the `iadc-advisor` Claude Code plugin, distributed from this repo acting as its own private marketplace.

**Architecture:** The plugin ships the *intelligence* (skills, a SessionStart hook, templates); each Appian app's repo holds the *per-project state* (`.mcp.json`, `docs/agents/*.md` config, `outputs/`), materialized by `/setup`. A SessionStart hook replaces the shipped `CLAUDE.md` (plugins cannot load one): it injects the operating posture plus the project's config files, with a gitignored `project.local.md` per-person override (Audience). No per-project value may live inside a `SKILL.md`.

**Tech Stack:** Claude Code plugin system (`.claude-plugin/plugin.json`, `marketplace.json`, hooks), bash (SessionStart hook script), Markdown skills.

## Global Constraints

- Plugin name is exactly `iadc-advisor` (skills namespace as `iadc-advisor:*`). Never rename after release.
- Versioning is **deliberate**: `"version"` in `plugin.json` + `CHANGELOG.md`. Never omit version (per-commit updates are rejected).
- Install scope for clients is **project scope** (committed `.claude/settings.json` in the app repo).
- MCP servers stay in a project-level gitignored `.mcp.json` written by `/setup` with **literal values** (Windows Desktop does not reliably expand `${VAR}`). The plugin manifest declares **no** `mcpServers`.
- Advisory posture is unchanged: no skill gains execution behavior.
- Clean break: no legacy/flattened-install migration code anywhere.
- The hook script must be POSIX-simple bash (Claude Code on Windows runs hooks via Git-for-Windows bash).
- Config precedence: `docs/agents/project.local.md` (gitignored, per-person) overrides `docs/agents/project.md` (committed) overrides built-in default (`Audience: developer`).
- Commit after every task. This repo pushes with the PAT in `.secrets/git-credentials` (see root `CLAUDE.md`) — but do **not** push during this plan; push is a separate release act.

---

### Task 1: Record the decisions — two ADRs + glossary re-cut

**Files:**
- Create: `docs/adr/0009-ship-as-claude-code-plugin.md` (0008 already exists — `0008-adr-per-decision-plus-ticket-rollup.md`)
- Create: `docs/adr/0010-no-config-in-skill-files.md`
- Modify: `CONTEXT.md`

**Interfaces:**
- Produces: the vocabulary later tasks must use — **Plugin**, **Per-project state**, **Session hook**, **Marketplace**. "Bundle" and "Deliverable" are retired.

- [ ] **Step 1: Write ADR 0009 (ship as plugin)**

```markdown
# The deliverable ships as a Claude Code plugin (`iadc-advisor`)

The product now ships as a Claude Code **plugin** named `iadc-advisor`, distributed from
this repo acting as its own private **marketplace** (`.claude-plugin/marketplace.json` at
the root points at the plugin folder). Clients add the marketplace once, then install the
plugin at **project scope** in each Appian-app repo (committed `.claude/settings.json`).
This supersedes the flatten-into-repo install: nothing is copied into the client repo by
hand anymore, and updates arrive by bumping `version` in `plugin.json` (deliberate
releases + `CHANGELOG.md`, never per-commit).

Driver: distribution and updates. The product stays conceptually **per-Appian-application**
— one app repo per application; the plugin is only the delivery vehicle.

Plugins cannot ship a loaded `CLAUDE.md`, so the operating posture moves to a
**SessionStart hook** that injects it, along with the project's configuration files
(`docs/agents/project.md`, overridden per-person by the gitignored
`docs/agents/project.local.md` — how a lead and a developer share one repo seamlessly).

MCP servers stay in a project-level gitignored `.mcp.json` generated by `/setup` from a
template that rides inside the plugin: the servers span per-app (`LCP_URL`, graph URL),
per-person (Appian credentials), and per-machine (`uv` path) values, which no
plugin-manifest mechanism (`userConfig` is plugin-wide per machine) can express — and the
docs explicitly steer self-hosted stdio servers to project `.mcp.json`.

## Considered options

- **Direct git install, no marketplace.** Rejected: loses update/versioning ergonomics
  and room for future plugins.
- **Separate marketplace repo.** Rejected: a second repo to maintain, for one plugin.
- **MCP servers in the plugin manifest via `userConfig`.** Rejected: wrong granularity
  (one value per machine across all apps) and re-risks the Windows `${VAR}` expansion bug
  that literal `.mcp.json` values were chosen to avoid. Future note: if the `iadc` graph
  is ever centrally hosted behind OAuth, it graduates into the manifest as a remote HTTP
  server — the ecosystem-preferred pattern.
- **User-scope install.** Rejected: the posture hook would fire in every unrelated project.

## Consequences

- ADR 0001's shipping story is amended: "only `deliverable/` ships" becomes "only the
  plugin folder ships", enforced by the marketplace `source` rather than by copying. The
  folder is renamed `iadc-advisor/`.
- `/setup` becomes the materializer of all per-project state (see ADR 0010): it writes
  `.mcp.json`, `docs/agents/*.md`, `.gitignore` entries, and `outputs/` into the app repo.
- Dogfooding changes: instead of opening Claude in the plugin folder, install the plugin
  from a local-path marketplace into a scratch client repo.
- No migration path: no flattened installs exist (clean break).
```

- [ ] **Step 2: Write ADR 0010 (no config in skill files)**

```markdown
# No per-project config lives in skill files

Plugin-delivered skills live in a shared, read-only cache (`~/.claude/plugins/cache/…`)
that is replaced on every update — so `/setup` can no longer write project values into
`SKILL.md` Configuration blocks (Appian version, app UUID, escalation target, office
profile), as it did when skills were copied into each app repo. One app's values would
also clobber another's.

**Supersedes ADR 0003** (store application identity in the graph skill): the application
name/nicknames/UUID now live in `docs/agents/project.md`, not `iadc-graph/SKILL.md` —
the no-live-lookup rule 0003 established is unchanged; only the storage location moves.

All per-project values move to **files in the app repo**: `docs/agents/project.md`
(committed team defaults), `docs/agents/project.local.md` (gitignored per-person
overrides — Audience), plus the existing `docs/agents/issue-tracker.md`,
`triage-labels.md`, `domain.md`. The plugin's SessionStart hook injects
`project.md` + `project.local.md` ambiently, so skills read config from context, exactly
as the shipped `CLAUDE.md` once made the Audience ambient.

Rule going forward: **a `SKILL.md` may carry knowledge, never configuration.** If a value
differs between two client apps, it belongs in `docs/agents/`, written by `/setup`.

## Considered options

- **Skills read config files on demand (no ambient injection).** Rejected: every skill
  grows a read step, and cross-cutting values (Audience, Appian version) stop being
  ambient, regressing behavior the shipped `CLAUDE.md` provided.
- **Plugin `userConfig`.** Rejected: plugin-wide per machine — wrong granularity for
  per-app values (see ADR 0009).

## Consequences

- `appian`, `iadc-graph`, `pressure-test`, and `office` lose their Configuration blocks
  and point at the ambient Project configuration instead.
- `/setup` writes `docs/agents/project.md` from a template asset and offers the
  per-person `project.local.md` override.
- The hook implements the local-over-committed precedence itself (Audience is not a
  native Claude Code settings key).
```

- [ ] **Step 3: Re-cut the glossary in `CONTEXT.md`**

Replace the **Bundle**, **Deliverable**, and **Workshop** entries (CONTEXT.md lines 12–24) with:

```markdown
**Plugin**:
The shippable artifact — the Claude Code plugin named `iadc-advisor`: everything under
`iadc-advisor/` (skills, the session hook, templates), installed by clients at project
scope from this repo's marketplace. The product this repo produces.
_Avoid_: bundle, deliverable (both retired with the flatten-into-repo install)

**Marketplace**:
This repo's `.claude-plugin/marketplace.json`, which lists the plugin. Clients add it
once (`/plugin marketplace add`), then install and update the plugin from it.

**Workshop**:
This repo, where the plugin is developed. Its root is the maintainer's dev
environment — auto-discovered by Claude Code — and is never shipped.

**Per-project state**:
Everything `/setup` materializes into a client app repo: the gitignored `.mcp.json`,
the `docs/agents/*.md` configuration (including `project.md` and the gitignored
per-person `project.local.md`), and the `outputs/` workspace. The plugin ships the
intelligence; the app repo holds the per-project state.

**Session hook**:
The plugin's SessionStart hook — the replacement for the shipped `CLAUDE.md` (plugins
cannot load one). Injects the operating posture plus `project.md`/`project.local.md`
into every session in an app repo where the plugin is enabled.
```

Also in `CONTEXT.md`, retire "bundle" everywhere:

- The H1: `# Appian Architect-in-a-Box — Bundle Design` → `# Appian Architect-in-a-Box — Plugin Design`.
- The intro paragraph: "building and maintaining this bundle *itself*" → "building and maintaining this plugin *itself*".
- **Dev docs** entry: "the bundle's maintainers" → "the plugin's maintainers".
- **Usage docs** entry: "when they use the bundle" → "when they use the plugin"; "the bundle never ships them" → "the plugin never ships them".
- **Advisory posture** entry: "the bundle only inspects" → "the plugin only inspects".
- **Handoff point** and the **Spine/Pressure-test/…** entries: replace each remaining "bundle" with "plugin".

Then confirm none remain: `grep -in bundle CONTEXT.md` → no output.

- [ ] **Step 4: Verify and commit**

Run: `ls docs/adr/0009-ship* docs/adr/0010-no-config* && grep -c "Plugin" CONTEXT.md && ! grep -qi "avoid.*plugin" CONTEXT.md && ! grep -qi "bundle" CONTEXT.md && echo OK`
Expected: both files listed, count ≥ 1, `OK`.

```bash
git add docs/adr/0009-ship-as-claude-code-plugin.md docs/adr/0010-no-config-in-skill-files.md CONTEXT.md
git commit -m "adr: ship as the iadc-advisor Claude Code plugin; config out of skill files"
```

---

### Task 2: Restructure to plugin layout — rename, manifest, marketplace

**Files:**
- Rename: `deliverable/` → `iadc-advisor/`; `iadc-advisor/.claude/skills/` → `iadc-advisor/skills/`
- Create: `iadc-advisor/.claude-plugin/plugin.json`
- Create: `.claude-plugin/marketplace.json` (repo root)
- Create/Modify: `.gitignore` (repo root)

**Interfaces:**
- Produces: plugin root `iadc-advisor/` with `skills/<name>/SKILL.md` layout; marketplace named `ignyte`; plugin installable as `iadc-advisor@ignyte`. All later tasks use these paths.

- [ ] **Step 1: Rename the folder and lift skills out of `.claude/`**

```bash
git mv deliverable iadc-advisor
git mv iadc-advisor/.claude/skills iadc-advisor/skills
rmdir iadc-advisor/.claude
```

Note: `iadc-advisor/.mcp.json` is untracked (gitignored, real credentials) — it stays on disk; Task 7 moves it into the dogfood repo. `git mv` won't touch it.

- [ ] **Step 2: Write the plugin manifest**

Create `iadc-advisor/.claude-plugin/plugin.json`:

```json
{
  "name": "iadc-advisor",
  "version": "1.0.0",
  "description": "Advisory Appian architect: pressure-test tickets against the live app, produce build specs, answer how-does-our-app-work — advise and plan, never build.",
  "author": { "name": "Ignyte Group" },
  "skills": "./skills/"
}
```

(No `hooks` key yet — Task 3 adds it together with the files it points at, so this task's `claude plugin validate` never sees a dangling path.)

- [ ] **Step 3: Write the marketplace**

Create `.claude-plugin/marketplace.json` at the repo root:

```json
{
  "name": "ignyte",
  "owner": { "name": "Ignyte Group" },
  "description": "Ignyte Group's Claude Code plugins.",
  "plugins": [
    {
      "name": "iadc-advisor",
      "source": "./iadc-advisor",
      "description": "Advisory Appian architect: pressure-test tickets against the live app, produce build specs, answer how-does-our-app-work — advise and plan, never build."
    }
  ]
}
```

- [ ] **Step 4: Update the root `.gitignore`**

The existing root `.gitignore`'s comments describe the old model ("the deliverable is in deliverable/ (which carries its own .gitignore)", "only .mcp.json.example ships, inside deliverable/"). Replace the whole file with:

```
# Workshop dev environment — the plugin ships from iadc-advisor/ (see docs/adr/0009).

# Secrets for workshop git operations — never commit
.secrets/

# Live MCP config holds literal credentials — never commit (the template ships
# inside the plugin as a /setup asset)
.mcp.json
iadc-advisor/.mcp.json

# OS / editor cruft
.DS_Store
Thumbs.db
```

- [ ] **Step 5: Verify and commit**

Run: `claude plugin validate iadc-advisor && claude plugin validate . && ls iadc-advisor/skills | wc -l && git status --porcelain | grep -c '^R' && git status --porcelain | grep -c '^ *D' || true`
Expected: both validations pass (no missing-description warnings); skill count `20`; rename count large (~100+); delete count `0`.

```bash
git add -A
git commit -m "restructure: deliverable/ becomes the iadc-advisor plugin; repo doubles as the ignyte marketplace"
```

---

### Task 3: The SessionStart hook — posture + project config injection

**Files:**
- Create: `iadc-advisor/hooks/hooks.json`
- Create: `iadc-advisor/hooks/session-start.sh`
- Create: `iadc-advisor/hooks/posture.md`
- Modify: `iadc-advisor/.claude-plugin/plugin.json` (add the `hooks` key)
- Delete: `iadc-advisor/CLAUDE.md` (its content dissolves into `posture.md` here and `/setup`/README in Tasks 5–6)

**Interfaces:**
- Consumes: nothing — the `posture.md` content (distilled from the deleted `CLAUDE.md`) is inlined in full at Step 3.
- Produces: ambient sections titled `# iadc-advisor — operating posture`, `## Project configuration` and `## Personal overrides` that Task 4's skill edits and Task 5's `/setup` refer to by name.

- [ ] **Step 1: Write `hooks/hooks.json`**

```json
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\""
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: Write `hooks/session-start.sh`**

```bash
#!/usr/bin/env bash
# iadc-advisor SessionStart hook — the plugin's replacement for a shipped CLAUDE.md
# (plugins cannot load one; see workshop ADR 0009). Injects the operating posture,
# then this project's configuration, with the gitignored per-person override last
# so its values take precedence (ADR 0010).
set -eu

plugin_root="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
project_dir="${CLAUDE_PROJECT_DIR:-.}"

cat "$plugin_root/hooks/posture.md"

if [ -f "$project_dir/docs/agents/project.md" ]; then
  printf '\n## Project configuration (from docs/agents/project.md — written by /setup)\n\n'
  cat "$project_dir/docs/agents/project.md"
else
  printf '\n## Project configuration\n\nNot configured yet — run /setup to wire the MCP servers and project values.\n'
fi

if [ -f "$project_dir/docs/agents/project.local.md" ]; then
  printf '\n## Personal overrides (from docs/agents/project.local.md — gitignored; these values OVERRIDE the project configuration above)\n\n'
  cat "$project_dir/docs/agents/project.local.md"
fi
```

Run: `chmod +x iadc-advisor/hooks/session-start.sh`

- [ ] **Step 3: Write `hooks/posture.md`** — the distilled `iadc-advisor/CLAUDE.md`

Carry over, condensed, from the deleted `CLAUDE.md` (drop: the "Skills" map — plugin skill descriptions are ambient and `/which-skill` routes; drop: "Configuration & secrets" — now `/setup`'s job; keep everything else):

```markdown
# iadc-advisor — operating posture

You are an **advisory Appian architect** for the Appian application this repo belongs to.
Your job: help a developer work out **how to build the ticket in front of them** — orient
in the app, sharpen the approach, leave an implementation note — and answer questions
about the application. **You do not write code or build Appian objects.** Everything here
is advisory: inspect, reason, plan, hand off.

New here? Run `/setup` first; ask `/which-skill` to route to the right flow.
Main flow: `ticket → /pressure-test → (/reconcile) → /to-spec` — the build spec.

## House rules

- **Audience.** Read the `Audience` line from the Project configuration below
  (default `developer`). Address the user as the person who will **implement** the
  ticket. When a decision needs authority they don't have — a genuine architectural
  gap — surface it and route it to the project lead (see `/pressure-test`). If
  `Audience` is `lead`/`architect`, the user owns those decisions — ask directly.
- **Advise, don't execute.** Never write application code or mutate Appian design
  objects. Produce specs, ADRs, ticket breakdowns, answers — not builds.
- **Read-only by default.** The Appian and graph MCPs are inspection-only; the
  Microsoft 365 surface is read-only.
- **Jira is human-first.** Read freely; every write (publish a breakdown, post a
  comment) is proposed first and confirmed per action.
- **Think before advising.** State assumptions; ask when uncertain; present
  interpretations and let the user choose.
- **One question at a time,** in plain language — the core interview discipline.
- **Ground answers in sources** — the graph, the live environment, the docs — and cite
  what you rely on.

## Data sources (wired by /setup; config in .mcp.json + connectors)

- **`iadc`** (graph MCP) — "what calls this", blast radius, record model. Load the
  `iadc-graph` skill before calling it.
- **`appian`** (MCP, read-only) — inspect the live environment. Load the `appian`
  skill first.
- **`context7`** (MCP) — semantic search over Appian docs. Load the `context7` skill.
- **Jira / Microsoft 365 / Slack** — Claude connectors (no `.mcp.json` entries): the
  board, SharePoint/OneDrive + Teams/Outlook (read-only), and gated escalation.

## Where things live in this repo

- `outputs/` (gitignored) — glossary `outputs/CONTEXT.md`, ADRs `outputs/adr/`,
  per-ticket `outputs/<TICKET-KEY>/` (decisions + build specs). See outputs/README.md.
- `docs/agents/` — this project's configuration: `project.md` (+ personal
  `project.local.md`), `issue-tracker.md`, `triage-labels.md`, `domain.md`.

## Working principles

- Simplicity first; answer at the right altitude.
- Scope, priorities, and trade-offs go to the user; implementation mechanics don't.
- Surface a simpler path when you see one; push back when warranted.
```

- [ ] **Step 4: Register the hooks in the manifest and delete the shipped `CLAUDE.md`**

In `iadc-advisor/.claude-plugin/plugin.json`, add after the `"skills"` line:

```json
  "hooks": "./hooks/hooks.json"
```

Then:

```bash
git rm iadc-advisor/CLAUDE.md
claude plugin validate iadc-advisor
```

Expected: validation passes with the hooks path now resolving.

- [ ] **Step 5: Verify the hook script end-to-end**

```bash
mkdir -p /tmp/hooktest/docs/agents
printf -- '- **Audience:** developer\n- **Appian version:** 26.6\n' > /tmp/hooktest/docs/agents/project.md
printf -- '- **Audience:** lead\n' > /tmp/hooktest/docs/agents/project.local.md
CLAUDE_PLUGIN_ROOT="$PWD/iadc-advisor" CLAUDE_PROJECT_DIR=/tmp/hooktest bash iadc-advisor/hooks/session-start.sh
```

Expected: posture text, then `## Project configuration` with both lines, then `## Personal overrides` with `Audience: lead`. Also run once with `CLAUDE_PROJECT_DIR=/tmp` → expected: the "Not configured yet — run /setup" fallback and **no** overrides section. Exit code 0 both times.

- [ ] **Step 6: Commit**

```bash
git add iadc-advisor/hooks iadc-advisor/.claude-plugin/plugin.json
git commit -m "hook: SessionStart injects posture + project config; retires shipped CLAUDE.md"
```

---

### Task 4: Config out of skill files — `appian`, `iadc-graph`, `pressure-test`, `office`

**Files:**
- Modify: `iadc-advisor/skills/appian/SKILL.md:34-46,226,232-233,289-291`
- Modify: `iadc-advisor/skills/appian/references/function-reference.md:100`
- Modify: `iadc-advisor/skills/appian/references/expressions.md:546,1679`
- Modify: `iadc-advisor/skills/iadc-graph/SKILL.md:47-78`
- Modify: `iadc-advisor/skills/pressure-test/SKILL.md:14-15,88,182-185`
- Modify: `iadc-advisor/skills/office/SKILL.md:30-49`
- Modify: `iadc-advisor/skills/orient/SKILL.md:42`
- Create: `iadc-advisor/skills/setup/project-config-template.md`

(Line numbers are pre-Task-2 positions; the files' content is unchanged by the rename, so locate by the quoted text.)

**Interfaces:**
- Consumes: the ambient section names from Task 3 (`## Project configuration`, `## Personal overrides`).
- Produces: `project-config-template.md`, which Task 5's `/setup` writes to `docs/agents/project.md`. Field names used by all four skills: `Audience`, `Appian version`, `Application` (name / nicknames / UUID), `Escalation` (channel / lead), `Office source of truth` (site / folder / active prospect).

- [ ] **Step 1: Write the project-config template** (`iadc-advisor/skills/setup/project-config-template.md`)

```markdown
# Project configuration — iadc-advisor

Written by `/setup`; injected into every session by the plugin's session hook. Committed —
team-shared defaults. Personal overrides go in `docs/agents/project.local.md` (gitignored,
same field names; its values win).

- **Audience:** developer <!-- developer | lead — who the advisor is talking to -->
- **Appian version:** <e.g. 26.6 — used for version-exact docs.appian.com lookups>
- **Application:** <full application name>
  - **Nicknames:** <e.g. CMS, "the case app"; optional>
  - **UUID:** `<application UUID — the iadc graph seed target>`
- **Escalation:** <Slack | Jira comment | hand-off>
  - **Project lead:** <Slack channel/handle, or Jira account>
- **Office source of truth:** <SharePoint site + pinned folder, or "none">
  - **Active prospect:** <profile name, if managing several from one instance>
```

- [ ] **Step 2: `appian/SKILL.md`** — replace lines 34–39 (`## Configuration` through the supported-versions list) with:

```markdown
## Configuration

**Appian version:** read the **`Appian version`** line from the ambient **Project
configuration** (injected at session start from `docs/agents/project.md`; written by
`/setup`). Never hardcoded here — this file is shared across projects.

**Supported Versions (as of 2026-07-15):**
26.6, 26.5, 26.3, 25.4, 25.3, 25.2, 25.1, 24.4, 24.3
```

Keep the following "Update this version…" prose but change "Update this version to match your Appian environment" → "Set it via `/setup` (or edit `docs/agents/project.md`)".

The same file has a second Configuration consumer — the "Appian Documentation Search" workflow. Four more edits, each shown as the exact before line(s) → after line(s):

Line 46 —

```
before: **To change:** Edit the version number above.
after:  **To change:** edit the `Appian version` line in `docs/agents/project.md`, or re-run `/setup`.
```

Line 226 —

```
before: Read the **Appian Version** from the Configuration section above. Use this version for all documentation URLs.
after:  Read the **`Appian version`** line from the ambient **Project configuration**. Use this version for all documentation URLs.
```

Lines 232–233 (inside the bash block) —

```
before: # Read configured version from Configuration section
        VERSION="26.6"  # Use the configured version
after:  # Use the Appian version from the ambient Project configuration
        VERSION="26.6"  # example — substitute the configured version
```

Lines 289–291 (the fallback — the old text told a client to edit the read-only plugin cache) —

```
before: If configured version is not found or Configuration section is missing:
        1. Default to version 26.6 (latest)
        2. Suggest to user: "Using Appian 26.6 docs. To use a different version, update the Configuration section in skills/appian/SKILL.md"
after:  If the Project configuration has no `Appian version` line:
        1. Default to version 26.6 (latest)
        2. Suggest to user: "Using Appian 26.6 docs. To use a different version, set the `Appian version` line in docs/agents/project.md or re-run /setup."
```

Two reference files under `appian/references/` point at the same section; repoint them:

- `references/function-reference.md:100`: "the Appian version configured in [SKILL.md Configuration section](../SKILL.md#configuration) (currently 26.6)" → "the Appian version from the ambient Project configuration (`docs/agents/project.md`)".
- `references/expressions.md:546`: the bash comment `# Read configured version from SKILL.md Configuration section` → `# Use the Appian version from the ambient Project configuration`.
- `references/expressions.md:1679`: "Uses the configured version from SKILL.md Configuration section." → "Uses the Appian version from the ambient Project configuration."

- [ ] **Step 3: `iadc-graph/SKILL.md`** — replace lines 47–59 (the Configuration block **plus** the "Seeding uses `seed(application_uuid="<the UUID above>")`… all live in this one block, so there's no cross-file lookup" paragraph, which references the deleted block) with:

```markdown
## Configuration

The graph's **seed target** comes from the ambient **Project configuration** (injected at
session start from `docs/agents/project.md`; written by `/setup`): the **`Application`**
entry — full name, **Nicknames**, and **UUID**. Read the UUID from there rather than
resolving it live through the Appian MCP.

Seeding uses `seed(application_uuid="<UUID from the Project configuration>")`. If a
developer names the app by a nickname, map it to the UUID via the **Nicknames** line
there — never via a live lookup.
```

Then rewrite the seeding prose (lines 64–74): every "Configuration block above/Configuration block" → "Project configuration"; "If the Configuration UUID is still a `<PLACEHOLDER>`" → "If the Project configuration has no Application UUID (or `/setup` hasn't run)". The do-NOT-call-listApplications rule keeps its exact force.

- [ ] **Step 4: `pressure-test/SKILL.md`** — replace lines 182–185 (`### Configuration` block) with:

```markdown
### Configuration

Escalation is configured in the ambient **Project configuration** (from
`docs/agents/project.md`, written by `/setup`): the **`Escalation`** line (channel —
Slack | Jira comment | hand-off) and **`Project lead`** (Slack channel/handle, or Jira
account). If unset, ask the user where escalations should go and suggest running `/setup`.
```

And two more edits in the same file:
- Line 88: "application UUID in the `/iadc-graph` Configuration block" → "application UUID in the Project configuration".
- Lines 14–15: "(the `Audience` in `CLAUDE.md` — developer unless configured otherwise)" → "(the `Audience` line in the ambient **Project configuration** — developer unless configured otherwise; a personal `project.local.md` override wins)". The shipped `CLAUDE.md` is deleted in Task 3 — left unfixed, this would point at the *client's* own unrelated `CLAUDE.md`.

- [ ] **Step 5: `office/SKILL.md`** — replace lines 30–49 exactly: from the `## Configuration` heading through the fallback sentence ending "…and offer to record it via `/setup`." (i.e. the intro prose, the `Active prospect` line, the `### Prospect profiles` table, and the use-the-matching-row/fallback paragraph). **Keep the "How to reach it:" paragraph (lines 51–55) unchanged** — the search-strategy guidance is knowledge, not config. Replacement:

```markdown
## Configuration

The advisor searches a **pinned source-of-truth folder** first, rather than scanning the
whole tenant. Read the **`Office source of truth`** entry (site + folder) and the
**`Active prospect`** line from the ambient **Project configuration** (from
`docs/agents/project.md`, written by `/setup`). Managing several prospects from one
instance? List each as a row under that entry in `project.md` and switch by changing the
**Active prospect** line — that single edit is the whole toggle.

If nothing is configured yet, ask the user for the site/folder (or search the tenant),
read from there, and offer to record it via `/setup`.
```

- [ ] **Step 6: `orient/SKILL.md`** — at line 42 change "its UUID is in the `/iadc-graph` Configuration block (set by `/setup`), so there's no live lookup" → "its UUID is in the ambient **Project configuration** (the `Application` entry, written by `/setup`), so there's no live lookup".

- [ ] **Step 7: Verify and commit**

Run: `grep -rn "set by /setup>\|PLACEHOLDER — set by /setup" iadc-advisor/skills/ | wc -l` → Expected: `0`.
Run: `grep -rn "Configuration block\|Configuration section" iadc-advisor/skills/ | wc -l` → Expected: `0` (the `## Configuration` *headings* stay; only "block/section" references die).
Run: `grep -rln "Project configuration" iadc-advisor/skills/{appian,iadc-graph,pressure-test,office,orient}/SKILL.md | wc -l` → Expected: `5`.

```bash
git add iadc-advisor/skills
git commit -m "skills: read per-project config from ambient Project configuration, not SKILL.md blocks (ADR 0010)"
```

---

### Task 5: Rewrite `/setup` — the materializer of per-project state

**Files:**
- Modify: `iadc-advisor/skills/setup/SKILL.md` (major rewrite of steps 1–3 and 7–8; steps 4–6 and 9 keep their substance)
- Rename: `iadc-advisor/.mcp.json.example` → `iadc-advisor/skills/setup/mcp-template.json`
- Rename: `iadc-advisor/outputs/README.md` → `iadc-advisor/skills/setup/outputs-readme.md`
- Delete: `iadc-advisor/outputs/.gitkeep`, `iadc-advisor/.gitignore`

**Interfaces:**
- Consumes: `project-config-template.md`, `mcp-template.json`, `outputs-readme.md` (skill assets); the hook's section names from Task 3.
- Produces: the client-repo contract every skill relies on — `.mcp.json` (gitignored), `docs/agents/project.md` (+ optional `project.local.md`), `docs/agents/{issue-tracker,triage-labels,domain}.md`, `outputs/` with README, `.gitignore` entries.

- [ ] **Step 1: Move the assets and drop shipped files**

```bash
git mv iadc-advisor/.mcp.json.example iadc-advisor/skills/setup/mcp-template.json
git mv iadc-advisor/outputs/README.md iadc-advisor/skills/setup/outputs-readme.md
git rm iadc-advisor/outputs/.gitkeep iadc-advisor/.gitignore
```

Then edit `mcp-template.json`'s `_comment` to: `"TEMPLATE — /setup asset. /setup writes the real .mcp.json (gitignored) into the app repo with LITERAL values, not ${VAR} — the Windows Desktop app does not reliably expand ${VAR} in .mcp.json."`
And in `outputs-readme.md` change "shippable bundle source" → "committed project files" and drop "remove the `/outputs/*` rule from `.gitignore`" → "remove the `outputs/` entries `/setup` added to `.gitignore`".

- [ ] **Step 2: Rewrite `setup/SKILL.md` frontmatter description and step 1 (Explore)**

Frontmatter `description`: `Configure the iadc-advisor plugin for this Appian project — write the per-project state into this repo (MCP servers + secrets, project configuration, issue tracker, domain docs), verify everything connects. Run once per app repo after installing the plugin, before first use of the other skills.` Keep `disable-model-invocation: true`.

Step 1 (Explore) — replace the bullet list with:

```markdown
- The plugin is installed at **project scope**: `.claude/settings.json` in this repo
  enables `iadc-advisor` (it's how this skill is running — verify, don't assume).
- `.mcp.json` at the repo root — exists? which servers, which values still placeholders?
- `docs/agents/` — `project.md`, `project.local.md`, `issue-tracker.md`,
  `triage-labels.md`, `domain.md` — which exist from a prior run?
- `outputs/` — does the workspace exist yet?
- `.gitignore` — does it have the entries listed in step 2c?
- `git remote -v` — is there a remote, and where?
```

- [ ] **Step 3: Rewrite step 2 (Wire the MCP servers) — generate, don't copy**

Replace the opening paragraph of step 2 with:

```markdown
The plugin talks to its data sources through MCP, configured **in this repo**, not in the
plugin. **Generate `.mcp.json` at the repo root from this skill's
[mcp-template.json](./mcp-template.json)** and fill in **literal** credential values —
not `${VAR}` (the Windows Desktop app does not reliably expand `${VAR}` in `.mcp.json`).
`.mcp.json` is gitignored (step 2c) so no secret is ever tracked; the template stays in
the plugin.

This is an existing app repo, not a fresh clone — respect what's already there:

- **`.mcp.json` already exists** → **merge, never overwrite**: add or update only the
  `iadc`, `appian`, and `context7` entries; preserve every other server the team has
  configured. Show the diff before writing.
- **`.mcp.json` is tracked in git** (`git ls-files --error-unmatch .mcp.json` succeeds) →
  stop and surface it: the team has committed it deliberately. Propose
  `git rm --cached .mcp.json` + the gitignore entry so credentials stop being tracked,
  but make no git change without an explicit yes — if they decline, warn that literal
  credentials must then never go in it, and record where the team wants secrets instead.
```

Keep the per-server bullets (`iadc`, `appian`, `context7`, Jira, Office, Slack connectors) unchanged except: in the Jira bullet, "the project key lives in `docs/agents/issue-tracker.md` (step 4), not an env var" stays; in the Office bullet, "(Its pinned source-of-truth folder is a project value — step 3.)" stays.

Add a new **step 2c — Materialize the workspace and ignores**:

```markdown
### 2c. Materialize the workspace and ignores

The plugin can't ship files into this repo, so create them here (idempotently — leave
existing content alone):

- **`outputs/`** — create the folder, write [outputs-readme.md](./outputs-readme.md) to
  `outputs/README.md`, and `touch outputs/.gitkeep`.
- **`.gitignore`** — ensure these entries exist (append any that are missing):

  ```
  # iadc-advisor per-project state — secrets and personal overrides, never committed
  .mcp.json
  docs/agents/project.local.md
  # generated planning artifacts (glossary, ADRs, specs) — working files, not source
  /outputs/*
  !/outputs/.gitkeep
  !/outputs/README.md
  ```
```

- [ ] **Step 4: Rewrite step 3 (Set project values) — one target file**

Replace step 3's five bullets' *write targets* (the values collected stay identical):

```markdown
Collect these and write them into **`docs/agents/project.md`**, from this skill's
[project-config-template.md](./project-config-template.md) — never into any `SKILL.md`
(plugin skills are shared and read-only; workshop ADR 0010):

- **Jira project key** — recorded in `docs/agents/issue-tracker.md` (step 4).
- **Appian version** — the `Appian version` line.
- **Application identity (graph seed target)** — full name, nicknames, and UUID on the
  `Application` entry. Resolve the UUID via the `appian` MCP (`listApplications`) or take
  it from the user — this is the one time a live lookup is worth it.
- **Office source-of-truth folder** (only if the M365 connector is used) — the
  `Office source of truth` entry (+ `Active prospect` if several).
- **Audience** — the `Audience` line: `developer` (default) or `lead`/`architect`.
- **Escalation target** (only if `/pressure-test` will escalate) — the `Escalation`
  entry: channel + project lead.

**Per-person override:** ask whether this user's role differs from the repo default
(e.g. a lead in a `developer`-default repo). If so, write just the differing lines to
**`docs/agents/project.local.md`** (gitignored — step 2c): same field names; the session
hook injects it after `project.md`, so its values win. Any teammate can do the same on
their machine without touching the committed default.
```

- [ ] **Step 5: Update steps 7–8 (Confirm/Verify)**

In step 7: drop the `## Agent skills` block in `CLAUDE.md` entirely (the plugin injects posture via its hook; a client `CLAUDE.md` is theirs, untouched). Show drafts of `docs/agents/*.md` including `project.md` before writing.
In step 8 (Verify), replace item 1 and 3 and add 4:

```markdown
1. **`.mcp.json` exists** (generated from the template), parses as JSON, has no
   `<placeholder>` strings left, and is matched by a `.gitignore` entry
   (`git check-ignore .mcp.json` succeeds).
3. **Project configuration is live** — `docs/agents/project.md` exists with no
   placeholders; if `project.local.md` was written, `git check-ignore` confirms it's
   ignored.
4. **The session hook fires** — tell the user to start a fresh session in this repo and
   confirm the "iadc-advisor — operating posture" and "Project configuration" sections
   appear at the top of context.
```

Delete old verify item 3 ("Skill frontmatter is valid" — plugin skills are validated at install, not per-project).

- [ ] **Step 6: Verify and commit**

Run: `grep -n "mcp.json.example\|Agent skills\|Configuration block of" iadc-advisor/skills/setup/SKILL.md | wc -l` → Expected: `0`.
Run: `ls iadc-advisor/skills/setup/` → Expected: includes `mcp-template.json`, `outputs-readme.md`, `project-config-template.md`, the four `issue-tracker-*.md`, `triage-labels.md`, `domain.md`, `SKILL.md`.

```bash
git add -A iadc-advisor
git commit -m "setup: materialize all per-project state into the app repo (mcp, project.md, gitignore, outputs)"
```

---

### Task 6: README, CHANGELOG, sweep the old vocabulary, workshop docs

**Files:**
- Modify: `iadc-advisor/README.md` (Getting started + Configuration sections)
- Create: `iadc-advisor/CHANGELOG.md`
- Modify: any skill still saying "bundle"/"clone"/pointing config at `CLAUDE.md` (sweep in step 3)
- Modify: `CLAUDE.md`, `README.md` (workshop root)

**Interfaces:**
- Consumes: marketplace name `ignyte`, plugin name `iadc-advisor`, `/setup` contract from Task 5.

- [ ] **Step 1: Rewrite README "Getting started" (lines 25–29) and "Configuration & secrets" (line 41–43)**

```markdown
## Getting started

1. **Add the marketplace** (once per machine): in any Claude Code session run
   `/plugin marketplace add <this repo's git URL>` — access uses the same credentials as
   the repo.
2. **Install the plugin in your Appian app's repo** (once per project, committed so the
   whole team gets it): open Claude Code in that repo, run `/plugin`, and install
   **iadc-advisor** from the **ignyte** marketplace at **project** scope. (Terminal
   alternative: `claude plugin install iadc-advisor@ignyte --scope project`.) Both work
   the same on the Windows Desktop app.
3. **Run `/setup`** in that repo. It generates the gitignored `.mcp.json` (graph, Appian,
   docs — literal values, no secrets tracked), writes the project configuration
   (`docs/agents/project.md` — Appian version, application UUID, audience, escalation),
   points the Jira/Microsoft 365 connectors, lays out the tracker + domain docs and the
   `outputs/` workspace — then verifies everything connects.
4. **Ask `/which-skill`** any time you're not sure which flow fits.

Updates: `claude plugin update iadc-advisor` — see `CHANGELOG.md` for what changed.

## Configuration & secrets

Nothing secret is ever committed: `/setup` writes real credentials only to the gitignored
`.mcp.json`, and your personal role override lives in the gitignored
`docs/agents/project.local.md`. Jira and Microsoft 365 connect as Claude connectors — no
tokens on disk. The committed `docs/agents/project.md` holds only team-shared,
non-secret values.
```

Elsewhere in README: replace "This is a Claude Code bundle" (line 3) → "This is a Claude Code plugin", and any "bundle" → "plugin" (`grep -n bundle iadc-advisor/README.md`).

- [ ] **Step 2: Write `iadc-advisor/CHANGELOG.md`**

```markdown
# Changelog — iadc-advisor

## 1.0.0 — 2026-07-23

First release as a Claude Code plugin (previously installed by copying files into the
app repo — no migration path; this is a clean break).

- Install from the `ignyte` marketplace at **project scope**, per Appian-app repo.
- Session hook injects the operating posture + `docs/agents/project.md`
  (+ personal `project.local.md` override — per-person Audience).
- `/setup` now materializes all per-project state: `.mcp.json`, project configuration,
  tracker/domain docs, `outputs/`, `.gitignore` entries.
- No per-project values live in skill files anymore.
```

- [ ] **Step 3: Sweep remaining old-model references in skills**

Run: `grep -rn -i "bundle\|clone this repo\|copied from .mcp.json.example\|\.claude/skills\|CLAUDE.md" iadc-advisor/skills iadc-advisor/README.md`
For each hit apply: "this bundle"/"the bundle"/"bundle's" → "this plugin"/"the plugin"/"plugin's"; "clone this repo" → "install the plugin (see README)"; paths `.claude/skills/…` → `skills/…` only where they refer to the plugin's own layout (references to a *client's* `.claude/` stay); any instruction to record or read a value in `CLAUDE.md` → the Project configuration / `docs/agents/project.md`. **Leave alone** the legitimate non-product uses of "bundle": "the extracted server bundle" / `<path-to-mcp-bundle-extracted>` (the LCP MCP zip, in `setup/SKILL.md` and `mcp-template.json`) and to-diagram's "platforms bundle/bundled their own Mermaid version". `which-skill`'s "configure this bundle" wording is caught by the sweep.

- [ ] **Step 4: Update the workshop root `CLAUDE.md`**

Rewrite the three stale sections to match the new model (keep Deploying as is):

```markdown
## The deliverable is the plugin — `iadc-advisor/`, and only it ships

The client receives the **`iadc-advisor` Claude Code plugin**: everything under
[`iadc-advisor/`](iadc-advisor/) — its `.claude-plugin/plugin.json`, `skills/`, `hooks/`,
and templates. This repo doubles as the private **marketplace**
(`.claude-plugin/marketplace.json` at the root). Shipping is still an allowlist: the
marketplace `source` points at `iadc-advisor/` and nothing else ships. Releases are
deliberate: bump `version` in `plugin.json` and record it in `iadc-advisor/CHANGELOG.md`.
See docs/adr/0001 and docs/adr/0009.

## Working here

- **Develop the plugin** by editing files under `iadc-advisor/`.
- **Dogfood / test as a client sees it** in the scratch client repo at
  `../iadc-dogfood` (a sibling of this repo, outside it — never inside the marketplace
  tree): add this repo as a local marketplace and install at project scope
  (`claude plugin marketplace add /home/saronsky/projects/IADC-Advisor`,
  then in `../iadc-dogfood`: `claude plugin install iadc-advisor@ignyte --scope project`),
  open Claude there, and run `/setup`. The session hook, namespaced skills, and
  per-project state behave exactly as they will for the client. After editing the
  plugin, refresh with `claude plugin marketplace update ignyte && claude plugin update
  iadc-advisor` and start a fresh session.
- **Never install or enable `iadc-advisor` in this repo itself** — its SessionStart
  hook would inject the advisory-architect posture ("you do not write code") into every
  maintainer session. Dogfood only in the scratch repo. (Since the restructure, the old
  `deliverable/`-scoped skills no longer auto-load in the workshop — expected.)
- **The advisory posture holds even in the workshop:** this repo produces docs,
  decisions, and configuration — it does not build client Appian objects.

## Extending the plugin

Author or edit skills with **`skill-creator`**. Keep skills **advisory**. When you add or
rename a skill, update **`which-skill`** (the router). Never put a per-project value in a
`SKILL.md` — that's ADR 0010; per-project state is written by `/setup` into the client
repo. Record hard-to-reverse decisions as ADRs in `docs/adr/`.
```

Also in the root `CLAUDE.md`'s "Dev docs" section: "which is exactly why they live in `deliverable/`, not here" → "which is exactly why the plugin's skills live in `iadc-advisor/`, not here".

- [ ] **Step 4b: Rewrite the root `README.md`** (the workshop-facing one — lines 3–9 describe the old model). Replace its body with:

```markdown
The development workshop for the **Appian Architect-in-a-Box** product — shipped as the
**`iadc-advisor` Claude Code plugin**, which turns Claude into an advisory Appian architect.

- **The plugin** — what ships to a client — lives entirely under
  [`iadc-advisor/`](iadc-advisor/); this repo doubles as its private marketplace
  (`.claude-plugin/marketplace.json`). The plugin's own README is the user-facing guide.
- **Dev docs** for maintainers live here at the root: `CONTEXT.md` (design vocabulary)
  and `docs/adr/` (decisions).
- **To try the plugin** as a client would, see "Working here" in `CLAUDE.md` — install it
  from this repo as a local marketplace into a scratch repo (never into this repo itself).

Only `iadc-advisor/` is shipped; everything else here is the workshop. See
[docs/adr/0001](docs/adr/0001-deliverable-lives-in-a-subfolder.md) and
[docs/adr/0009](docs/adr/0009-ship-as-claude-code-plugin.md).
```

(Keep the H1 title line as is.)

- [ ] **Step 5: Verify and commit**

Run: `grep -rn -iE "this bundle|the bundle|bundle's|Claude Code bundle|configure this bundle" iadc-advisor/ CLAUDE.md README.md .gitignore | grep -v CHANGELOG | wc -l` → Expected: `0`. (Plain `\bbundle\b` hits legitimate uses — the LCP "extracted server bundle", `<path-to-mcp-bundle-extracted>`, to-diagram's Mermaid "bundle/bundled" — which must stay.)
Run: `grep -rn "deliverable/" README.md CLAUDE.md .gitignore | wc -l` → Expected: `0` (ADR files keep their historical wording).

```bash
git add iadc-advisor/README.md iadc-advisor/CHANGELOG.md iadc-advisor/skills CLAUDE.md README.md
git commit -m "docs: plugin-era READMEs + changelog; retire bundle vocabulary; workshop docs"
```

---

### Task 7: Dogfood — install from the local marketplace and verify the whole seam

> **Human-driven acceptance task — do NOT dispatch to a subagent.** Steps 3–4 need an
> interactive Claude session, real endpoints/credentials, and human observation of the
> injected context. The maintainer runs this; the executor's job ends at Task 6.

**Files:**
- Create: `../iadc-dogfood/` (scratch client repo — a **sibling** of this repo, outside it: adding this repo as a local marketplace snapshots its tree into the plugin cache, so neither the scratch repo nor the real-credential `.mcp.json` may live inside it)

**Interfaces:**
- Consumes: everything — this is the acceptance test for Tasks 2–6.

- [ ] **Step 1: Create the scratch client repo and park the real credentials there**

```bash
mkdir -p ../iadc-dogfood && cd ../iadc-dogfood && git init -q
mv ../IADC-Advisor/iadc-advisor/.mcp.json ./.mcp.json   # the untracked real-credential file from the old layout — OUT of the marketplace tree
cd ../IADC-Advisor
```

- [ ] **Step 2: Add the local marketplace and install at project scope**

```bash
claude plugin marketplace add /home/saronsky/projects/IADC-Advisor
cd ../iadc-dogfood && claude plugin install iadc-advisor@ignyte --scope project
```

Expected: install succeeds; `../iadc-dogfood/.claude/settings.json` now enables `iadc-advisor`.

- [ ] **Step 3: Verify the session seam** (interactive — do this in a new Claude session in `../iadc-dogfood/`)

Checklist, each observable in that session:
1. Top of context contains `# iadc-advisor — operating posture` and the "Not configured yet — run /setup" fallback (no `project.md` yet).
2. Skills list shows the namespaced skills (`iadc-advisor:pressure-test`, `iadc-advisor:setup`, … 20 of them).
3. Run `/setup` and complete it against the real endpoints (the parked `.mcp.json` values are the answers). Confirm it writes: `.mcp.json` (regenerated), `docs/agents/project.md`, `.gitignore` entries, `outputs/README.md`; `git check-ignore .mcp.json docs/agents/project.local.md` both succeed.
4. Start another fresh session: the `## Project configuration` section now shows the real values. Write `- **Audience:** lead` to `docs/agents/project.local.md`, restart, and confirm the `## Personal overrides` section appears after it.
5. Spot-check one config-reading skill: ask `/iadc-graph` to seed — it must take the UUID from the ambient Project configuration without calling `listApplications`.

- [ ] **Step 4: Fix–refresh loop**

Fix whatever step 3 surfaced (in `iadc-advisor/`, committing per fix). A local marketplace is **snapshotted** at add/install time — after each fix, refresh before retesting:

```bash
claude plugin marketplace update ignyte && claude plugin update iadc-advisor
```

…then start a fresh session in `../iadc-dogfood/`. If the update no-ops because the version is unchanged at `1.0.0` (deliberate-versioning behavior), either bump `plugin.json` to a `1.0.1-dev` pre-release while iterating (reset to the release number before shipping) or `claude plugin uninstall iadc-advisor && claude plugin install iadc-advisor@ignyte --scope project`.

Run (in the workshop repo): `git status --porcelain | wc -l` → Expected: `0`.

---

## Explicitly out of scope (decided)

- No migration from flattened installs (clean break — none exist).
- No `mcpServers` in the plugin manifest; revisit only if the `iadc` graph is hosted behind OAuth (noted in ADR 0009).
- No per-commit auto-updates; releases are version bumps.
- Pushing/releasing 1.0.0 (deploying with the PAT) is a separate act after the dogfood passes.
