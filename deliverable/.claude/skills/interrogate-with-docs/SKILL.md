---
name: interrogate-with-docs
description: Interrogate a plan, design, or decision one question at a time to sharpen it, capturing the resulting glossary and decisions to the git-ignored outputs workspace (outputs/CONTEXT.md + ADRs). The default interrogation path when the project has (or should have) a domain model. Use when the user wants to stress-test or sharpen their thinking, or uses any 'interrogate' trigger phrase.
---

Run a `/interrogating` session, using the `/domain-modeling` skill.
