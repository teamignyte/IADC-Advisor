# Workshop dev environment — the deliverable is in deliverable/ (which carries its own .gitignore).

# Secrets for workshop git operations — never commit
.secrets/

# OS / editor cruft
.DS_Store
Thumbs.db
