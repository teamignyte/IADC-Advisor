# Workshop dev environment — the plugin ships from iadc-advisor/ (see docs/adr/0009).

# Secrets for workshop git operations — never commit
.secrets/

# Live MCP config holds literal credentials — never commit (the template ships
# inside the plugin as a /setup asset)
.mcp.json
iadc-advisor/.mcp.json

# OS / editor cruft
.DS_Store
Thumbs.db

# Project-local git worktrees (see family CLAUDE.md, Working across repos)
.worktrees/
